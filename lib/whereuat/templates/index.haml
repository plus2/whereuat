- if @stories.length != 0
  %ol
    - @stories.each do |story|
      %li
        %a.accept{:href => "#accept"} &radic;
        %a.reject{:href => "#reject"} &chi;
        %a.story{:href => story.url}= story.name
        %span.story-id.hide= story.id
- else
  %ol
    %li.no-stories There are no stories for you to test at this time.
