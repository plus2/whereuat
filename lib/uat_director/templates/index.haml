%ol
  - @stories.each do |story|
    %li
      %a.accept{:href => "#accept"} Accept
      %a.reject{:href => "#reject"} Reject
      %a{:href => story.url}= story.name
      %span.story-id.hide= story.id